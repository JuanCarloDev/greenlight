# AGENTS.md · greenlight

> Contrato mínimo cross-LLM (Claude, Cursor, Copilot, Gemini, humanos). Repo da Hive, padrão DX.
> `CLAUDE.md` é o superset disto pro Claude Code.

## Padrão DX da Hive
- **`whiteboard.md`** = quadro de status do Juan (AGORA · PRA TI · PRÓXIMO · FEITO · DECISÕES), mantido proativo.
- **`tasks/`** = backlog executável (1 task = 1 arquivo, com "como verificar").
- **Memória** isolada por repo.

## Regras (globais do Juan)
push só com confirmação · nunca force push · nunca `.env`/credentials/keys no git · jamais travessão (—) nem en-dash (–) em nada · E2E antes de declarar pronto · sem ego · altitude CTO.
